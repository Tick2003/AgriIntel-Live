# AgriIntel.in — ETL-only dependencies (used by daily_update.yml CI workflow)
# Minimal set required to run etl/data_loader.py + its full import chain.
# Includes: data (pandas/numpy/scipy), HTTP (requests/feedparser/bs4),
#           ML (scikit-learn/xgboost), NLP (nltk), auth (bcrypt),
#           stats (statsmodels), serialisation (joblib).
# Does NOT include: mlflow, streamlit, catboost, lightgbm, plotly, fastapi.

pandas==2.1.3
numpy==1.26.2
requests==2.32.4
feedparser==6.0.12
scikit-learn==1.5.0
xgboost==3.1.3
scipy==1.13.1
nltk==3.10.3
statsmodels==0.14.4
joblib==1.4.2
beautifulsoup4==4.14.3
bcrypt==5.0.0
