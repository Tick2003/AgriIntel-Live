# AgriIntel.in — ETL-only dependencies (used by daily_update.yml CI workflow)
# Minimal set required to run etl/data_loader.py + its agent imports.
# Keep this lean — the full stack (mlflow, streamlit, catboost etc.) is NOT needed here.

pandas==2.1.3
numpy==1.26.2
requests==2.31.0
feedparser==6.0.12
scikit-learn==1.5.0
xgboost==3.1.3
scipy==1.13.1
nltk==3.10.3
